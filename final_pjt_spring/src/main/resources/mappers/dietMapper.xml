<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper
  PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN"
  "https://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="com.workout.model.dao.DietDao">

    <!-- Diet 등록 -->
    <insert id="insertDiet" parameterType="Diet">
        INSERT INTO diet (text1, image1, text2, image2, text3, image3)
        VALUES (#{text1}, #{image1}, #{text2}, #{image2}, #{text3}, #{image3})
    </insert>

    <!-- Diet 수정 -->
    <update id="updateDiet" parameterType="Diet">
        UPDATE diet
        SET text1 = #{text1}, image1 = #{image1}, 
            text2 = #{text2}, image2 = #{image2}, 
            text3 = #{text3}, image3 = #{image3}
        WHERE diet_id = #{dietId}
    </update>

    <!-- Diet 삭제 -->
    <delete id="deleteDiet" parameterType="Long">
        DELETE FROM diet WHERE diet_id = #{dietId}
    </delete>

</mapper>
