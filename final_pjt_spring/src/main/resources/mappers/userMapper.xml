<?xml version="1.0" encoding="UTF-8" ?>
<!DOCTYPE mapper
  PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN"
  "https://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="com.workout.model.dao.UserDao">
	<!-- 사용자 조회 (비밀번호 제외) -->
    <select id="selectOne" parameterType="Map" resultType="User"> 
        SELECT user_id, name, birthDate, gender, email, username, role
        FROM users
        WHERE username = #{username} AND password = #{password}
    </select>
    
    <!-- 회원가입 -->
    <insert id="insertUser" parameterType="User">
        INSERT INTO users (name, birthDate, phoneNum, gender, email, username, password, role, security_question, security_answer)
        VALUES (#{name}, #{birthDate}, #{phoneNum}, #{gender}, #{email}, #{username}, #{password}, #{role}, #{securityQuestion}, #{securityAnswer})
    </insert>
    
    <!-- 아이디 찾기 -->
    <select id="findByEmailAndName" resultType="User">
    	SELECT username, security_question, security_answer FROM users
    	WHERE email = #{email} AND name = #{name}
    </select>
    
    <select id="selectByEmail" resultType="User">
    	SELECT * FROM users
    	WHERE email = #{email}
    </select>
    
</mapper>
