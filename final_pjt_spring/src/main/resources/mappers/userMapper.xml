<?xml version="1.0" encoding="UTF-8" ?>
<!DOCTYPE mapper
  PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN"
  "https://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="com.workout.model.dao.UserDao">
	<!-- 사용자 조회 (비밀번호 제외) -->
    <select id="selectOne" parameterType="Map" resultType="User"> 
        SELECT username, name, email
        FROM users
        WHERE username = #{username} AND password = #{password}
    </select>
    
    <!-- 회원가입 -->
    <insert id="insertUser" parameterType="User">
        INSERT INTO users (name, age, gender, email, username, password, role, preferred_exercise)
        VALUES (#{name}, #{age}, #{gender}, #{email}, #{username}, #{password}, #{role}, #{preferred_exercise})
    </insert>
</mapper>
